# building.yml
name: building_docker

# Allow repository modyfication;
permissions:
  contents: write
  pull-requests: write
  packages: write

on:
  # Allow to re-use this jobs;
  workflow_call:
  # Trigger on each push;
  push:
  # Triger manualy;
  workflow_dispatch:

jobs:
  building_docker_github: 
    uses: ./.github/workflows/building_docker_github.yml@develop
