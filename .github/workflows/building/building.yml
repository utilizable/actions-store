# building.yml
name: building 

# Allow repository modyfication;
permissions:
  contents: write
  pull-requests: write
  packages: write

on:
  # Allow to re-use this jobs;
  workflow_call:
  # Trigger on each push;
  push:
  # Triger manualy;
  workflow_dispatch:

jobs:

  github: 
    uses: ./building/docker/github/github.yml
